#pragma once
#include <iostream>
#include <string>
#include <vector>

using namespace std;

void AddValue(int new_data)
{
	
	LinkedList* new_node;
	LinkedList* current;
	LinkedList* head;
		struct Node* new_node = (struct Node*)
	   current-> previous = new_data;
		new_node->next = head;
		head = new_node;
	}
		//empty list?
		//if (head == nullptr)
		//{
		//	head = new LinkedList();
		//}
		//else
		//{
		//	//one or more boxes already in our list
		//	//go to end of list to add new value
		//	LinkedList* current = head;

		//	//go to end of list
		//	while (current->next != nullptr)
		//	{
		//		current = current->next;
		//	}

		//	//create a new box for our data
		//	LinkedList* new_box = new LinkedList { a };

		//	//add the box to the linked list
		//	current->next(new_box);
		//}

}